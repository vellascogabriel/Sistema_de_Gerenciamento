{% extends "layout.njk" %}

{% block content%}
    <div class = 'card'>
        <section 
        class="avatar" 
        style = " width:350px; background: url({{ cliente.avatar_url}}) no-repeat center center / cover "></section>
        <section class="details">
            <h3>Cliente</h3>

            <div class = "item">
                <div> Cliente </div>
                <div>{{cliente.name}}</div>
            </div>

            <div class = "item">
                <div>Idade</div>
                <div>{{cliente.birth}}</div>
            </div>

            <div class = "item">
                <div>Aniversário</div>
                <div>{{cliente.age}}</div>
            </div>

            <div class = "item">
                <div>email</div>
                <span>{{ cliente.email }}</span>
            </div>

            <a href="/clientes/{{cliente.id}}/edit" class = "button">Editar</a>
        </section>
    </div>
{% endblock content %}